const api=require('../../utils/api');const images={北京:'/assets/city-beijing.jpg',成都:'/assets/city-chengdu.jpg',重庆:'/assets/city-chongqing.jpg',杭州:'/assets/city-hangzhou.jpg',西安:'/assets/city-xian.jpg'}
Page({data:{guides:[]},onShow(){this.load()},async load(){try{const cities=await api.get('/app/voyaai/cities',{});this.setData({guides:cities.map((x,i)=>({cityId:x.id,city:x.name,image:x.coverImage?api.imageUrl(x.coverImage):images[x.name]||images.成都,title:`${x.name}${i+3}天${i%2?'深度':'自由行'}攻略`,views:`${(12.6-i*1.3).toFixed(1)}k`,likes:856-i*97}))})}catch(e){wx.showToast({title:e.message||'加载失败',icon:'none'})}},openSearch(){wx.navigateTo({url:'/pages/search/search'})},openGuide(e){const x=e.currentTarget.dataset.city;const item=this.data.guides.find(y=>y.cityId===x);wx.navigateTo({url:`/pages/attractions/attractions?cityId=${x}&cityName=${encodeURIComponent(item?item.city:'城市')}`})}})
